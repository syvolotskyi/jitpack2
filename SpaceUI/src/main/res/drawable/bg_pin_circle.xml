<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Space contains text -->
    <!-- PinEntryEditText has error set -->
    <item android:state_active="true">
        <shape android:shape="oval">
            <stroke android:width="1dp" android:color="?attr/status_primary_distractive" />
            <solid android:color="?attr/status_primary_distractive" />
        </shape>
    </item>

    <item android:state_checked="true" android:state_focused="true">
        <shape android:shape="oval">
            <solid android:color="?attr/brand_primary" />
        </shape>
    </item>
    <item android:state_checked="true">
        <shape android:shape="oval">
            <solid android:color="?attr/brand_primary" />
        </shape>
    </item>
    <!--Space is the next space for character input -->
    <item android:state_focused="true" android:state_selected="true">
        <shape android:shape="oval">
            <stroke android:width="1dp" android:color="?attr/brand_primary" />
            <solid android:color="?attr/white" />
        </shape>
    </item>
    <!--Space is the next space for character input -->
    <item android:state_selected="true">
        <shape android:shape="oval">
            <stroke android:width="1dp" android:color="?attr/brand_primary" />
            <solid android:color="?attr/white" />
        </shape>
    </item>
    <!-- PinEntryEditText is focused -->
    <item android:state_focused="true">
        <shape android:shape="oval">
            <stroke android:width="1dp" android:color="?attr/brand_primary" />
            <solid android:color="?attr/white" />
        </shape>
    </item>

    <!-- PinEntryEditText is not focused -->
    <item >
        <shape android:shape="oval">
            <stroke android:width="1dp" android:color="?attr/brand_primary" />
            <solid android:color="?attr/white" />
        </shape>
    </item>

</selector>
